import React from 'react';
import styled from 'styled-components';

const ContentContainer = styled.div`
  align-items: center;
`;

function TestResult() {
  return <ContentContainer>테스트결과 페이지...</ContentContainer>;
}

export default TestResult;
